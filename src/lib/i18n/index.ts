import { dictionnaire } from "./fr";

// Une seule langue pour l'instant (voir fr.ts). Pour ajouter une langue plus tard :
// créer en.ts avec la même forme, puis choisir ici le dictionnaire selon la locale de l'utilisateur.
export const d = dictionnaire;
